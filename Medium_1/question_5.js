// What will the following two lines of code output?

console.log(0.3 + 0.6);
console.log(0.3 + 0.6 === 0.9);

// Don't look at the solution before you answer.

/**
 * Solution:
 * The above lines will log `0.9` and then `true` to the console.
 *
 * Correction:
 * The above code logs `0.8999999999999999` and `false` to the console.
 */